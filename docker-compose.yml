services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cbb_analytics
      POSTGRES_USER: cbb_user
      POSTGRES_PASSWORD: cbb_password
    networks:
      web:
        aliases:
          - macfax_db 
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cbb_user -d cbb_analytics"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    hostname: macfax_api
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      DATABASE_URL: postgres://cbb_user:cbb_password@db:5432/cbb_analytics
      DEBUG: "False"
      SECRET_KEY: change-me
      ALLOWED_HOSTS: "localhost,127.0.0.1,macfax.usu.edu"
      CORS_ALLOWED_ORIGINS: "http://localhost:7001,http://macfax.usu.edu,https://macfax.usu.edu"
      CSRF_TRUSTED_ORIGINS: "http://localhost:7000,http://localhost:7001,http://macfax.usu.edu,https://macfax.usu.edu"
    depends_on:
      db:
        condition: service_healthy
    networks:
      web:
        aliases:
          - macfax_api
    ports:
      - "7001:7001"
    command: ["sh", "-c", "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:7001"]

  web:
    hostname: macfax_web
    build:
      context: .
      dockerfile: web/Dockerfile
      args:
        # Baked into the client bundle at build time — must be the external URL
        NEXT_PUBLIC_API_BASE_URL: "https://macfax.usu.edu"
    environment:
      NODE_ENV: production
      PORT: 7000
      # Server-side only (not exposed to browser) — internal Docker hostname
      API_INTERNAL_URL: "http://macfax_api:7001"
      # Fallback public URL for server-side if internal isn't set
      NEXT_PUBLIC_API_BASE_URL: "https://macfax.usu.edu"
    depends_on:
      - backend
    ports:
      - "7000:7000"
    networks:
      web:
        aliases:
          - macfax_web

networks:
  web:
    external: true

volumes:
  postgres_data: